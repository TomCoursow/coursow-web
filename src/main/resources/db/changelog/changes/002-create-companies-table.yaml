databaseChangeLog:
  - changeSet:
      id: 002-create-companies-table
      author: tom-coursow
      preConditions:
        - onFail: MARK_RAN # Already in PROD when Liquibase was added to the project
        - not:
            tableExists:
              tableName: companies
      changes:
        - createTable:
            tableName: companies
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: VARCHAR(255)
              - column:
                  name: timeframe
                  type: VARCHAR(255)
              - column:
                  name: description
                  type: TEXT
              - column:
                  name: title_color_class
                  type: VARCHAR(255)
